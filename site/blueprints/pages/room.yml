title: Meeting Room
columns:
  main:
    width: 2/3
    fields:
      rmnum:
        label: Library Calendar Room Number
        type: number
      authurl:
        label: Calendar API URL
        type: text
  sidebar:
    width: 1/3
    fields:
      qr_image:
        type: files
        query: page.images
        multiple: false
        label: Room QR Code

